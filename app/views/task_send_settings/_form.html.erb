<%= form_with(model: task_send_setting) do |form| %>
  <% if task_send_setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task_send_setting.errors.count, "error") %> prohibited this task_send_setting from being saved:</h2>

      <ul>
        <% task_send_setting.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :space_id %>
    <%= form.text_field :space_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
